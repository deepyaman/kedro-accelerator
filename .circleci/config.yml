version: 2.1

orbs:
  python: circleci/python@0.3.2

jobs:
  build-and-test:
    parameters:
      dependency-file: src/requirements.txt
    executor: python/default
    steps:
      - checkout
      - python/load-cache
      - python/install-deps
      - python/save-cache
      - run:
          command: kedro test
          name: Test

workflows:
  main:
    jobs:
      - build-and-test
